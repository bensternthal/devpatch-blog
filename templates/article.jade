
extends layout
include date

block prepend title
  | #{ page.title } |

block header

block content
  h1= page.title
  p
      em= moment.utc(page.metadata.date).format('MMMM DD, YYYY')
  article.post
    section.content!= page.html
  include comments.html
